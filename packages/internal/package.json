{
  "name": "@cue/internal",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Cue Console — cuesea staff only. Deliberately a separate build from Cue Studio so no customer ever receives this bundle.",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/sync-docs.mjs && vite build",
    "preview": "vite preview",
    "docs:sync": "node scripts/sync-docs.mjs"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.0"
  }
}
