{
  "name": "@gapvision/pos-app",
  "private": true,
  "version": "0.1.0",
  "description": "Cue POS Handoff — a Shopify POS UI extension that pulls the Lens's live engagement into the till. Deliberately NOT in the monorepo's npm workspaces: the Shopify CLI owns this package's install and build, and its dependency tree should not leak into the plugin or server builds.",
  "scripts": {
    "dev": "shopify app dev",
    "deploy": "shopify app deploy",
    "link": "shopify app config link"
  },
  "dependencies": {
    "preact": "^10.24.0"
  },
  "devDependencies": {
    "@shopify/ui-extensions": "2026.7.1",
    "typescript": "^5.5.0"
  }
}
