fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic[email]>=2.7
psycopg[binary,pool]>=3.2
cryptography>=42.0
zxing-cpp>=2.2
Pillow>=10.3
python-barcode>=0.15
# Provisioning QR codes, generated at mint. Already the plate generator's QR
# library — one QR implementation in the product, not two.
segno>=1.6
